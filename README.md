# Socket_Communication_ServerApp

This project implements HTTP server interface. Server listens for msg value sent from the browser and give response in JSON format. It supports responses like server name, current status, time, or all details together.